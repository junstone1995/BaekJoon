#include<stdio.h>
#define max 101
int main(void)
{
    int i,cnt,count = 0;
    char word[max];
    scanf("%d",&cnt);
    for(i=0;i<cnt;i++)
        {scanf("%s",word);
        for(j=0; j<sizeof(word);j++)
            dfdf
        }
}